PKG_CPPFLAGS = -I/opt/homebrew/include -I./include -DHAVE_FLINT
PKG_LIBS = -L/opt/homebrew/lib -lflint -lgmp -lmpfr -lm

# C source files to compile
OBJECTS = init.o r_interface.o builtin_functions.o

# Use C11 standard to avoid warnings about C11 extensions
PKG_CFLAGS = -DHAVE_VISIBILITY_ATTRIBUTE
